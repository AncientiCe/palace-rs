# Mission

`mempalace-rs` provides local-first memory for AI assistants.

The project prioritizes:

- Verbatim storage over lossy summaries.
- Local operation by default.
- A small Rust API that can be embedded in other services.
- Clear MCP tools for assistants that need retrieval and durable memory.
